;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Only tomorrow
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
(set-info :k 5)
(declare-tfun x () Bool)
(assert (not x))
(assert (next x))
(assert (not (next (next x))))
(get-model)